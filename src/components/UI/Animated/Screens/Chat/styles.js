import { StyleSheet } from 'react-native';

export const chatStyles = StyleSheet.create({
  mainContainer: {
    position: 'absolute',
    width: '100%',
    height: '100%',
    backgroundColor: 'blue'
  }
});